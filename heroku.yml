build:
  docker:
    web: Dockerfile
  config:
    POSTGRES_DB: ${JDBC_DATABASE_URL}
    POSTGRES_USERNAME: ${JDBC_DATABASE_USERNAME}
    POSTGRES_PASSWORD: ${JDBC_DATABASE_PASSWORD}
release:
  image: web