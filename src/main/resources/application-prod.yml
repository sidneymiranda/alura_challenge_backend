spring:
  datasource:
    url: ${DATABASE_URL}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    url: ${DATABASE_URL}
#    user: ${JDBC_DATABASE_USERNAME}.
#    password: ${JDBC_DATABASE_PASSWORD}
    locations: classpath:db/migration
    enabled: true
