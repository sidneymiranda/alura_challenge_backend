spring:
  datasource:
    url: ${DATABASE_URL}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    open-in-view: false
flyway:
  enable: true
  check-location: false
  locations: classpath:db/migration
